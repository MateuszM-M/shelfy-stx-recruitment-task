    # if 'title' in queries:
    #     f"+intitle:{queries['title']}"
    # if 'author' in queries:
    #     f"+inauthor:{queries['author']}"
    # if 'isbn' in queries:
    #     f"+isbn:{queries['isbn']}"